## 项目启动说明

### 启动命令

```bash
npm run dev
```

执行该命令即可启动本地开发服务器，项目将在开发模式下运行。

### 环境要求

- Node.js 版本: `22.16.0`

### 注意事项

- 确保已安装项目依赖（可通过 `npm install` 安装）
- 启动后可在浏览器中访问项目，默认端口通常为 3000 或 8080
- 开发模式下支持热更新，代码修改后会自动刷新页面